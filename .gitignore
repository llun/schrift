# macOS
.DS_Store

# Xcode / XcodeGen
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# XcodeGen-generated Info.plist template (see project.yml's `info:` block) — like
# the .xcodeproj, this is fully reproduced from project.yml by `xcodegen generate`.
/Generated/

# Swift Package Manager
.swiftpm/
Package.resolved
.build/

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/test_output/
fastlane/README.md
build/

# Signing secrets — NEVER commit these
*.p8
*.p12
*.mobileprovision
*.cer

# Local, machine-specific signing config (holds your DEVELOPMENT_TEAM).
# Signing.xcconfig (committed) optionally includes this; see it for details.
Local.xcconfig

# Session-local scratch written by the superpowers agent workflow (task
# briefs/reports, progress notes) — never current state, never committed.
.superpowers/
